# ZHKU 校园网自动登录 — 使用手册

## 简介

本程序自动检测仲恺农业工程学院校园网（Drcom 认证系统）连接状态，断线时自动重新登录，保持网络持续在线。适合需要长时间挂机、无人值守的场景。

**原理：** 每隔 5 秒访问一次外部检测 URL（`http://www.gstatic.com/generate_204`），如果网络正常返回 204，程序休眠等待；如果被重定向到认证页面（Captive Portal），说明需要重新登录，程序自动提交登录请求。

---

## 一、获取程序

### 方式 A：下载打包好的 .exe（推荐）

从 [Releases](https://github.com/Abiu/ZHKU-Connector/releases) 下载最新版本，包含两个文件：

| 文件 | 说明 |
|------|------|
| `ZHKU校园网自动登录.exe` | 主程序 |
| `config.yml` | 配置文件 |

将两个文件放在**同一个文件夹**中即可。

### 方式 B：从源码运行（需要 Python 3.6+）

```bash
# 1. 克隆仓库
git clone https://github.com/Abiu/ZHKU-Connector.git
cd ZHKU-Connector

# 2. 安装依赖
pip install requests progress pyyaml termcolor

# 3. 运行
python main.py
```

### 方式 C：在 Windows 上自行打包

```bash
# 下载源码后，双击运行：
打包程序.bat

# 或手动执行：
python build.py
```

打包完成后，可执行文件在 `dist/` 目录下，与 `config.yml` 一起分发即可。

> **注意：** 打包必须在 Windows 系统上进行，macOS/Linux 上无法生成 .exe 文件。

---

## 二、首次配置

### 1. 编辑 config.yml

用记事本打开 `config.yml`，确认以下配置：

```yaml
current_version: version_abiu
last_update: 2025-05
home_page: https://github.com/Abiu/ZHKU-Connector
detect_captive_portal_url: http://www.gstatic.com/generate_204
login_page: http://172.31.255.1/drcom/login?callback=dr1003&DDDDD={user_id}&upass={password}&0MKKey=123456&R1=0&R2=&R3=0&R6=0&para=00&v6ip=&terminal_type=1&lang=zh-cn&jsVersion=4.2&v=2579&lang=zh
printable: True
```

| 配置项 | 说明 |
|--------|------|
| `detect_captive_portal_url` | 用于检测网络连通性的 URL，通常不需要修改 |
| `login_page` | 校园网认证页面地址，**{user_id}** 和 **{password}** 是占位符，请勿删除 |

### 2. 首次运行

双击 `ZHKU校园网自动登录.exe`，按提示输入：

```
请输入账号：你的学号
请输入密码：你的校园网密码
```

登录成功后，程序会询问是否保存登录信息。选择 **Y**（是）后，下次启动将自动使用保存的凭据。

> **提示：** 凭据保存在 `C:\Users\你的用户名\network_credentials.pkl`，如需清除可删除该文件。

---

## 三、日常使用

### 启动

双击 `ZHKU校园网自动登录.exe`。

- **有保存的凭据：** 程序显示 5 秒倒计时，等待后自动使用——按回车可取消并重新输入。
- **无保存的凭据：** 直接提示输入账号密码。

### 运行状态

程序窗口会持续显示监测状态：

- `网络已连通 ⠋` — 网络正常，程序在后台监测
- 当检测到断线时，自动执行登录并显示结果

### 退出

直接关闭窗口（点 X）或按 `Ctrl+C`。

### 开机自启（可选）

1. 右键 `ZHKU校园网自动登录.exe` → 创建快捷方式
2. `Win+R` 输入 `shell:startup` → 回车
3. 将快捷方式拖入打开的文件夹

---

## 四、常见问题

### Q: 程序闪退？
**A:** 检查 `config.yml` 是否与 .exe 在同一文件夹。如果缺少配置文件，程序会闪退。

### Q: 一直提示"登录失败"？
**A:**
1. 确认账号密码正确（先在浏览器手动登录一次试试）
2. 确认连接的是校园网 WiFi，而非其他网络
3. 检查 `config.yml` 中 `login_page` 地址是否仍然有效（学校可能会更新认证页面地址）

### Q: macOS/Linux 能用吗？
**A:** 不能用打包好的 .exe，但可以直接运行源码：
```bash
pip install requests progress pyyaml termcolor
python main.py
```
注意：macOS/Linux 版不支持按回车取消自动登录（该功能依赖 Windows 专属 API）。

### Q: 密码安全吗？
**A:** 凭据保存在用户主目录下的 pickle 文件中，未加密。建议不要在公共电脑上使用"记住登录信息"功能。

---

## 五、项目信息

- 原作者：Jin-Cheng Ming ([ZHKU-Connector](https://github.com/Jin-Cheng-Ming/ZHKU-Connector))
- Fork 作者：Abiu ([ZHKU-Connector](https://github.com/Abiu/ZHKU-Connector))
- 许可证：MIT
